<!DOCTYPE html>
<html>
<head>
  <title>Simpleefeen</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body>
<main class="container flex">
  <div class="flex-none flex-col bg-blue-600">
    <div class="py-2">
      <%= link_to "Movements", movements_path, class: "w-full text-center py-3 rounded bg-blue-600 text-white hover:bg-green-dark focus:outline-none" %>
    </div>
    <div class="py-2">
      <%= link_to "Assets", assets_path, class: "w-full text-center py-3 rounded bg-blue-600 text-white hover:bg-green-dark focus:outline-none" %>
    </div>
    <div class="py-2">
      <%= link_to "Categories", categories_path, class: "w-full text-center py-3 rounded bg-blue-600 text-white hover:bg-green-dark focus:outline-none" %>
    </div>
    <div class="py-2">
      <%= link_to "Graphs", graphs_path, class: "w-full text-center py-3 rounded bg-blue-600 text-white hover:bg-green-dark focus:outline-none" %>
    </div>
    <div class="py-2">
      <%= link_to "Import", import_path, class: "w-full text-center py-3 rounded bg-blue-600 text-white hover:bg-green-dark focus:outline-none" %>
    </div>
  </div>
  <div class="container mx-auto mt-28 px-5 flex">
    <%= yield %>
  </div>
</main>
</body>
</html>
